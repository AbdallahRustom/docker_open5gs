logger:
    file: /open5gs/install/var/log/open5gs/scp.log

sbi:
    server:
      no_tls: true
    client:
      no_tls: true

parameter:

scp:
    sbi:
      dev: eth0

nrf:
    sbi:
      - addr:
          - NRF_IP

